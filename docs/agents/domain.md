# Domain docs

The Planilla web app has one domain context. Before changing it, read:

- `CONTEXT.md`
- relevant records in `docs/adr/`

Use the glossary's canonical vocabulary in issues, plans, tests, and code. If a proposal conflicts with an ADR, surface the conflict rather than silently overriding it.
